# Atlas Stack Status
Generated by scaffolder. Read this before writing any code.

## Verdict: EXISTING — preserve current stack

## Detected stack
- Runtime: FastAPI (Python)
- ORM: asyncpg (direct PostgreSQL driver, no ORM)
- Auth: python-jose + Authlib (JWT-based)
- Health checks: Present at /api/health (standard FastAPI)
- K8s service name: whisper-analyzer (per CLAUDE.md)

## Frontend
- Type: Standalone HTML (static/index.html, ~12,000 lines)
- Framework: Vanilla JavaScript
- Not a separate SPA — served directly by FastAPI

## Rules for all agents
- Do not migrate the ORM
- Do not replace the auth implementation
- Do not change the framework
- Do not scaffold a separate web frontend (Next.js, Angular, etc.) — the frontend lives in static/index.html
- Only add features within the existing patterns
